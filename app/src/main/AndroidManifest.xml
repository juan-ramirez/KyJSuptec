<?xml version="1.0" encoding="utf-8"?>
<manifest package="app.kyjsuptec.kjingenieros"
          xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true"/>
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>sic
    <uses-permission android:name="android.permission.WRITE_CONTACTS"/>

    <application
        android:name=".basic.CustomApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.UserLoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".activities.MainActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.CrearModificarUsuarioActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.AdministrarUsuariosActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.FormularioActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".activities.MemoExpressActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.EnviarFormulariosActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.CameraActivity"
            android:label="Proyectos"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".activities.LocalUserInitActivity"
            android:label="@string/title_activity_local_user_init"
            android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>
